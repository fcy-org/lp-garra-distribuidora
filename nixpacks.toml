[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "HOST=0.0.0.0 NITRO_HOST=0.0.0.0 node dist/server/index.mjs"
