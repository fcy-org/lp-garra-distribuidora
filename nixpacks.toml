[variables]
HOST = "0.0.0.0"
NITRO_HOST = "0.0.0.0"
PORT = "3000"

[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
